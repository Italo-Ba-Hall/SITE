═══════════════════════════════════════════════════════════════════════════════
                         COMANDOS RÁPIDOS - DEPLOY
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. REFAZER BUILD DO FRONTEND (se precisar)                                 │
└─────────────────────────────────────────────────────────────────────────────┘

cd frontend
npm run build


┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. TESTAR BACKEND LOCALMENTE (antes de fazer deploy)                       │
└─────────────────────────────────────────────────────────────────────────────┘

cd backend
venv\Scripts\activate
python start_server.py

# Testar em outro terminal:
curl http://localhost:8000/health


┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. DEPLOY BACKEND NO RAILWAY                                               │
└─────────────────────────────────────────────────────────────────────────────┘

# Instalar CLI (só precisa fazer uma vez):
npm install -g @railway/cli

# Deploy:
cd backend
railway login
railway init
railway up

# Adicionar variáveis no painel web:
# GROQ_API_KEY=sua_chave
# FRONTEND_URL=http://barrahall.dev.br


┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. VERIFICAR SE ESTÁ FUNCIONANDO                                           │
└─────────────────────────────────────────────────────────────────────────────┘

# Backend:
curl https://sua-url-backend/health

# Frontend:
# Abrir no navegador: http://barrahall.dev.br
# Digitar mensagem e ver se chat abre


┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. ATUALIZAR .ENV E REFAZER BUILD (depois de deploy do backend)            │
└─────────────────────────────────────────────────────────────────────────────┘

# 1. Editar frontend/.env.production
# Colocar: REACT_APP_API_URL=https://sua-url-backend-aqui

# 2. Refazer build:
cd frontend
npm run build

# 3. Re-enviar arquivos do build/ para o FTP


═══════════════════════════════════════════════════════════════════════════════
                            ARQUIVOS IMPORTANTES
═══════════════════════════════════════════════════════════════════════════════

📁 frontend/build/                    → Enviar para FTP /www
📄 DEPLOY_INSTRUCTIONS.md             → Guia completo
📄 FTP_FILES_CHECKLIST.txt            → Lista de arquivos
📄 RESUMO_DEPLOY.txt                  → Visão geral
📄 frontend/.env.production           → Config frontend produção
📄 backend/.env.production.example    → Template config backend


═══════════════════════════════════════════════════════════════════════════════
                                  AJUDA RÁPIDA
═══════════════════════════════════════════════════════════════════════════════

❓ Frontend não carrega?
   → Verificar se index.html está em /www (não em subpasta)
   → Verificar se pasta /static/ foi enviada

❓ Chat não abre?
   → Verificar URL do backend em .env.production
   → Refazer build depois de atualizar .env
   → Ver erros no console do navegador (F12)

❓ Erro de CORS?
   → Verificar FRONTEND_URL no backend
   → Confirmar que URL do FTP está correta no backend

❓ Backend não responde?
   → Verificar logs no Railway/PythonAnywhere
   → Confirmar que GROQ_API_KEY está configurada
   → Testar endpoint /health

